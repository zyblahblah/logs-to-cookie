# nixpacks.toml — Railway build configuration
# Installs system packages needed for archive extraction.
# p7zip handles encrypted .zip and .7z files.
# unrar handles encrypted .rar files.
[phases.setup]
nixPkgs = ["p7zip", "unrar"]
